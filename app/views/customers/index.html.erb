<h1><%= t(:myprofile)%></h1>

<table>
  <tr>
    <th><%= t(:nickname)%></th>
    <th><%= t(:credit) %></th>
    <th><%= t(:maintenance) %></th>
    <th><%= t(:status)%></th>
    <th><%= t(:user)%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @customers.each do |customer| %>
  <tr>
    <td><%= customer.nickname %></td>
    <td><%= customer.credits %></td>
    <td><%= customer.maintenance %></td>
    <td><%= customer.Status %></td>
    <td><%= customer.user.id %></td>
    <td><%= link_to t(:show), customer %></td>
    <td><%= link_to t(:editcustomer), edit_customer_path(customer) %></td>
    <td><%= link_to t(:destory), customer, :confirm => t(:ays), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(:newcustomer), new_customer_path %>
